<p id="notice"><%= notice %></p>

<h1>Listing Softs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th>Maker</th>
      <th>Hard</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @softs.each do |soft| %>
      <tr>
        <td><%= soft.name %></td>
        <td><%= soft.price %></td>
        <td><%= soft.maker %></td>
        <td><%= soft.hard %></td>
        <td><%= link_to 'Show', soft %></td>
        <td><%= link_to 'Edit', edit_soft_path(soft) %></td>
        <td><%= link_to 'Destroy', soft, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Soft', new_soft_path %>
